<h1>Список кусочков</h1>

<table class="table">
  <tr>
    <th><%= t(:az_label_name) %></th>
    <th>.</th>
    <th>.</th>
    <th>.</th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= h project.name %></td>
    <td><%= link_to t(:az_label_edit), edit_az_project_snippet_path(project) %></td>
    <td><%= link_to t(:az_label_show), az_project_snippet_path(project) %></td>
    <td><%= link_to t(:az_label_delete), project, :confirm => t(:az_label_are_you_sure), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новый кусочек', new_az_project_snippet_path %>
