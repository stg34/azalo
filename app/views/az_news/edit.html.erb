<h1>Редактирование новости</h1>

<% form_for(@az_news) do |f| %>
  <%= f.error_messages %>

  <%= render( :partial => 'new_edit', :locals => {:form => f}) %>
  
  <p>
    <%= f.submit t(:az_label_save) %>
  </p>
<% end %>

<%= link_to t(:az_label_show), @az_news %> |
<%= link_to t(:az_label_back), az_news_path %>
