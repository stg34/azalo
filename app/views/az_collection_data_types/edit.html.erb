<%= crumbs(@az_collection_data_type.get_crumbs_to_parent) %>

<h1>Редактирование коллекции</h1>

<%= form_for(@az_collection_data_type) do |f| %>
  <%= f.error_messages %>
 
  <%= render( :partial => 'new_edit', :locals => {:form => f}) %>

  <p>
    <%= f.submit t(:az_label_save) %>
  </p>
<% end %>

